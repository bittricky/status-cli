{
  "name": "down-cli",
  "version": "0.0.1",
  "description": "Simple Command Line tool to check if a website is down",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "down": "cli.js"
  },
  "author": "Mitul Patel",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.2.0"
  }
}
